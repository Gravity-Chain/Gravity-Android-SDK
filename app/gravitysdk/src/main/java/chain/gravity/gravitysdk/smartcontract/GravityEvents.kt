package chain.gravity.gravitysdk.smartcontract

import java.math.BigDecimal

class GravityEvents() {
    fun transfer(
        from: String,
        to: String,
        tokens: BigDecimal
    ) {
    }

    fun approve(
        from: String,
        spender: String,
        tokens: BigDecimal
    ) {
    }
}